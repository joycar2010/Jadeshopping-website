# Server environment example
PORT=8080
JWT_SECRET=dev-secret-change-me

# Default admin seed
ADMIN_DEFAULT_USERNAME=admin
ADMIN_DEFAULT_EMAIL=admin@example.com
ADMIN_DEFAULT_PASSWORD=123456

# PostgreSQL connection
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/jadeshopping?schema=public

# Optional: Protect migration API for production
ADMIN_MIGRATION_TOKEN=

# Supabase service credentials (required for admin operators)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=